name: Build

on:
  push:
    branches:
    - "*"
    - "!release/v*"

jobs:
  check_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Init
        run: yarn

      - name: Build
        run: yarn run build:libs -- --prod
